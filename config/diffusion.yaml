data:
  data_dir: data
  augment: true
  batch_size: 8

model:
  base_ch: 128
  time_dim: 64

diffusion:
  timesteps: 1000
  beta_start: 1.0e-4
  beta_end: 2.0e-2

optimization:
  lr: 1.0e-4
  weight_decay: 0.0
  clip_grad_norm: 1.0

training:
  steps: 10000
  save_every: 1000
  ema_decay: 0.9999
  condition_dropout: 0.1

output:
  vae_run_dir: run/20260714-202424-225286
  run_root: run
