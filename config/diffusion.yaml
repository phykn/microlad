data:
  data_dir: data
  augment: true
  batch_size: 8

model:
  base_ch: 128
  time_dim: 64

diffusion:
  timesteps: 1000
  beta_start: 1.0e-4
  beta_end: 2.0e-2

optimization:
  lr: 1.0e-4
  weight_decay: 0.0
  clip_grad_norm: 1.0

training:
  steps: 50000
  save_every: 5000

output:
  vae_run_dir: run/20260712-120359-181168
  run_root: run
