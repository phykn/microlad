data:
  data_dir: data
  crop_size: 128
  segment: true
  augment: true
  batch_size: 8

model:
  base_ch: 128
  time_dim: 64

diffusion:
  timesteps: 1000
  beta_start: 1.0e-4
  beta_end: 2.0e-2

optimization:
  lr: 1.0e-4
  weight_decay: 0.0
  clip_grad_norm: 1.0

training:
  steps: 50000
  save_every: 1000

output:
  vae_run_dir:
  run_root: run/20260706-145958-609474
