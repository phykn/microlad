data:
  data_dir: data/train
  fake_dir: fake
  augment: true
  batch_size: 8

model:
  noise_ch: 128
  generator_ch: 64
  critic_ch: 64

loss:
  gp_weight: 10.0

optimization:
  generator_lr: 1.0e-4
  critic_lr: 1.0e-4
  betas: [0.0, 0.9]
  clip_grad_norm: null

training:
  steps: 10000
  critic_steps: 5
  save_every: 1000

output:
  vae_run_dir: run/20260715-114554-625954
  run_root: run
