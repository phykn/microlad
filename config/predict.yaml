models:
  vae_run_dir: run/20260714-202424-225286
  diffusion_run_dir: run/20260714-204642-744006
  gan_run_dir: run/20260714-212941-630628

progress: true
phase_fractions: null
segment_anchors: false

prior:
  weight: 1.0
  anchor_strength: 0.25
  t_min: 10
  t_max: 200

targets:
  segment: false
  slice_fraction_weight: 0.0
  global_fraction_weight: 5.0
  tpc_weight: 0.0
  surface_area_weight: 0.0
  diffusivity_weight: 0.0
  diffusivity_grid_size: null
  low_phase_conductivity: 0.0

joint:
  steps: 700
  batch_size: 1
  # Use null to decode each axis in one batch on a large-memory GPU.
  decode_batch_size: 16
  learning_rate: 0.0003
  axis_weight: 1.0
  axis_mass_weight: 0.25
  continuity_weight: 0.05
  anchor_weight: 2.0
  residual_scale: 2.0
  preservation_weight: 1.0

critic:
  weight: 0.1

scale:
  overlap: 0.25
  steps: 500
  batch_size: 8
  # Use null to decode each stage in one batch on a large-memory GPU.
  decode_batch_size: 16
  learning_rate: 0.003
  anchor_weight: 2.0
  continuity_weight: 0.05
  preservation_weight: 1.0
  residual_scale: 2.0

refine:
  enabled: true
  batch_size: 16
  strength: 0.15
  anchor_strength: 0.05
